version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto11

  build:
    commands:
      - ls -la *
      - chmod +x ./gradlew
      - ./gradlew clean
      #- ./gradlew assembleAndroidTest
      - ./gradlew assembleDebug
artifacts:
  files:
    - './MyApplication/app/build/outputs/**/*.apk'
  discard-paths: yes